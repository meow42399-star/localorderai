<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.CALL_PHONE" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />

    <queries>
        <intent>
            <action android:name="android.intent.action.CALL" />
        </intent>
        <intent>
            <action android:name="android.intent.action.DIAL" />
        </intent>
    </queries>

    <application
        android:name=".LocalOrderAiApp"
        android:label="LocalOrderAI PRO"
        android:icon="@mipmap/ic_launcher"
        android:allowBackup="false"
        android:supportsRtl="true"
        android:theme="@style/Theme.LocalOrderAI">

        <activity
            android:name=".ui.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".ui.AutoCallLauncherActivity"
            android:exported="false"
            android:theme="@android:style/Theme.Translucent.NoTitleBar"
            android:launchMode="singleInstance"
            android:excludeFromRecents="true"
            android:noHistory="true" />

        <!--
            foregroundServiceType="dataSync" هنا مش دقيق دلاليًا (الخدمة
            فعليًا بتدير حملة اتصال، مش مزامنة بيانات)، لكنه النوع الوحيد
            اللي بيشتغل بمجرد FOREGROUND_SERVICE العادي. أي نوع أدق زي
            "phoneCall" بيتطلب من أندرويد 14 (targetSdk 34+) واحد من:
            MANAGE_OWN_CALLS permission، أو التطبيق يكون System Dialer
            فعليًا. المشروع ده شال فكرة Default Dialer بالكامل (بيستخدم
            TelephonyManager + ACTION_CALL بس)، فمفيش شرط بيستوفيه لأي
            نوع أدق — استخدام phoneCall هنا كان يسبب SecurityException
            فوري ويقفل الحملة قبل ما تبدأ.
        -->
        <service
            android:name=".services.CampaignForegroundService"
            android:exported="false"
            android:foregroundServiceType="dataSync" />

        <service
            android:name=".services.OverlayBubbleService"
            android:exported="false" />

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="com.localorderai.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

    </application>
</manifest>
